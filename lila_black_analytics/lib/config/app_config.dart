class AppConfig {
  // Configurable base URL. Replace with actual production URL or environment variable.
  static const String apiBaseUrl = 'http://127.0.0.1:8000';
  
  // Frame update interval for playback (~60fps)
  static const int playbackTickMs = 16;
}